import { ComponentFixture, TestBed } from '@angular/core/testing';

import { MyReportViewerComponent } from './my-report-viewer.component';

describe('MyReportViewerComponent', () => {
  let component: MyReportViewerComponent;
  let fixture: ComponentFixture<MyReportViewerComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      imports: [MyReportViewerComponent]
    })
    .compileComponents();
    
    fixture = TestBed.createComponent(MyReportViewerComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});