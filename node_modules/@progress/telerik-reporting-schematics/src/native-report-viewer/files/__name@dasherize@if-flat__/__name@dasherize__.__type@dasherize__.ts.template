import { <% if(changeDetection !== 'Default') { %>ChangeDetectionStrategy, <% }%>Component<% if(!!viewEncapsulation) { %>, ViewEncapsulation<% }%> } from '@angular/core';<% if(standalone) {%>
import { CommonModule } from '@angular/common';<% } %><% if (standalone) { %>
import { ReportingAngularViewerModule } from '@progress/telerik-angular-native-report-viewer';<% } %>

@Component({<% if(!skipSelector) {%>
  selector: '<%= selector %>',<%}%><% if(standalone) {%>
  standalone: true,
  imports: [CommonModule<% if (standalone) { %>, ReportingAngularViewerModule<% } %>],<%}%><% if(inlineTemplate) { %>
  template: `
    <reporting-angular-viewer
        #report
        [reportSource]="{
            report: '<%= report %>',
            parameters: {}
        }"
        serviceUrl="<%= serviceUrl %>"
        viewMode="interactive"
        [keepClientAlive]="true">
    </reporting-angular-viewer>
  `<% } else { %>
  templateUrl: './<%= dasherize(name) %><%= type ? '.' + dasherize(type): '' %>.html'<% } if(inlineStyle) { %>,
  styles: [`<% if(displayBlock){ %>
    :host {
      display: block;
    }
  <% } %>`]<% } else if (style !== 'none') { %>,
  styleUrls: ['./<%= dasherize(name) %><%= type ? '.' + dasherize(type): '' %>.<%= style %>']<% } %><% if(!!viewEncapsulation) { %>,
  encapsulation: ViewEncapsulation.<%= viewEncapsulation %><% } if (changeDetection !== 'Default') { %>,
  changeDetection: ChangeDetectionStrategy.<%= changeDetection %><% } %>
})
export class <%= classify(name) %><%= classify(type) %> {

}
